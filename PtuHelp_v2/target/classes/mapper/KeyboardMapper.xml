<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PtuHelp.dao.KeyboardDao">

    <!-- MBTI 유형에 맞는 키보드 조회 -->
    <select id="getKeyboardsByMbtiType" parameterType="string" resultType="PtuHelp.model.Keyboard">
        SELECT K_ID AS keyboardId, type, url, Keyname AS keyname
        FROM Keyboards
        WHERE type = #{mbtiType}
    </select>

    <!-- 전체 키보드 목록 조회, 평점 포함 -->
    <select id="selectAllKeyboards" resultType="PtuHelp.model.Keyboard">
    	SELECT K_ID AS keyboardId, 
               type, 
               url, 
               Keyname AS keyname,
               COALESCE((SELECT AVG(rating) FROM KeyboardRatings WHERE keyboardId = K_ID), 0) AS averageRating
        FROM Keyboards
	</select>
    
    <select id="selectKeyboardById" parameterType="int" resultType="PtuHelp.model.Keyboard">
        SELECT K_ID AS keyboardId, type, url, Keyname AS keyname FROM Keyboards WHERE K_ID = #{keyboardId}
    </select>
    
    <!-- 평점 삽입 또는 업데이트 -->
    <insert id="upsertRating">
        INSERT INTO KeyboardRatings (userId, keyboardId, rating)
        VALUES (#{userId}, #{keyboardId}, #{rating})
        ON DUPLICATE KEY UPDATE rating = #{rating}
    </insert>

    <!-- 키보드 평점 평균 조회 -->
    <select id="getAverageRatingByKeyboardId" parameterType="int" resultType="double">
        SELECT AVG(rating) FROM KeyboardRatings WHERE keyboardId = #{keyboardId}
    </select>
    
</mapper>
